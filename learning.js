/*
    ARRAY METHODS()
        map()
        -Function that copies all elements in an array and makes a new one.
        
        flat(depth) 
        -Takes an array that has elements that are arrays and concatenates the array into a new array.
        -The depth parameter specifies "how deep a nested function should be flattened."

        flatMap()
        -The same as using .map(.flat()) with a depth level of 1. 
        -Copies array and concatenates all elements.

        
*/